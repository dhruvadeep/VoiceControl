[project]
name = "logging-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.8",
    "loguru>=0.7.3",
    "toml>=0.10.2",
    "uvicorn>=0.34.0",
]
